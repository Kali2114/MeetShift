misconfigurations:
  - id: AWS-0104
    statement: EC2 requires outbound internet access for AWS SSM, Docker image pulls, package updates, and SMTP email delivery.

  - id: AWS-0131
    statement: Existing EC2 root volume is unencrypted. Encryption will be handled during a planned instance migration to avoid replacing the production instance unexpectedly.
